
@{
                ViewBag.Title = "ProjectAssignment";
}

<div class="container">
    <div class="row">
        <div class="col-md-2">
        </div>
        <div class="col-md-10">
            <div class="row">
                <h2>Project Assignment</h2>
            </div>
        </div>

        <div class=" row">
            <div class="col-md-6">
                @Html.Label("SelectProject")
            </div>
            <div class="col-md-6">
                @Html.Label("SelectUsers")
            </div>
        </div>

        @Html.BeginForm()
        {
        @Html.AntiForgeryToken()
        <div class=" row">
            <div class="col-md-6">
                @Html.DropDownList("Projects", null, htmlAttributes: new { @class = "form-control" })
            </div>
            <div class="col-md-6">
                @Html.DropDownList("Users", null, htmlAttributes: new { @class = "form-control" })
            </div>
        </div>
        <div class=" row">
            <div class="col-md-12 text-center">
                <button class="btn btn-primary" type="submit">Save<</button>
            </div>
        </div>        
    </div>
</div>
